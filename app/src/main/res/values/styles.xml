<resources>
    <!-- Button Styles -->
    <style name="GradientButton" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/gradient_button_background</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- TextView Styles -->
    <style name="Header.TextStyle" parent="android:Widget.TextView">
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Body.TextStyle" parent="android:Widget.TextView">
        <item name="android:textColor">@color/textColorSecondary</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- CardView Style -->
    <style name="Modern.CardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">8dp</item>
        <item name="android:layout_margin">8dp</item>
    </style>
</resources>
